A generated Python test file passes coverage but leaves surviving mutants.
Strengthen assertions and edge-case checks without changing production code.

Stable rules:
- Edit tests only, under `tests/uta_generated/`.
- Do not edit production Python files.
- Preserve deterministic execution and UTA ownership metadata.
- Do not assert implementation details unless behavior cannot otherwise kill the mutant.
- Mock side effects; never rely on real network, database, scheduler, or clock state.
- When the source path is under `src/`, add the repository `src` directory to `sys.path` and import the real package below it, for example `shidiao.routes`; do not import modules with a `src.` prefix and do not create a synthetic alias package such as `_uta_*` because mutmut must see tests execute the real mutated module.

{# CACHE_BOUNDARY #}
### TARGET
- Target: `{{ display_name }}`
- Target ID: `{{ target_id }}`
- Source: `{{ source_path }}`
{% if symbol %}- Symbol: `{{ symbol }}`
{% endif %}
- Generated test path: `{{ generated_test_path or test_file_path }}`
- Mutation gate: `{{ mutation_gate or ci_diff_mutation_gate }}`

### CONTEXT
- Target context: `{{ context_abs or target_context_abs }}`
- Context JSON: `{{ context_json_abs }}`
- Structured query: `{{ index_query_command }}`

### SURVIVING MUTANTS
```
{{ mutation_diagnostics or mutation_report }}
```

### REPAIR TASK
Update the same generated test file to kill the listed surviving mutants with behavior-focused assertions.
