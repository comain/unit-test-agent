"""Where UTA decides which concrete pieces this deployment uses.

Composition is allowed to name concrete things; nothing below it is. That is
the whole reason this package exists as a layer of its own rather than as a
module inside one of the layers it composes.
"""
