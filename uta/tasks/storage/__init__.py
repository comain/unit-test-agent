"""`TaskDB`'s repositories, split by what they store.

Nothing here is meant to be constructed on its own -- they are mixins that
assume `TaskDB`'s connection and transaction. Import `TaskDB` from
`uta.tasks.db`, which is where it has always been.
"""
