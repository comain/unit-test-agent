"""Consumer-side persistence ports for the test-generation workflow.

Declared as a package for the same reason as the Java binding: namespace
packages behave differently at build time and inconsistently with the rest of
the tree.
"""
